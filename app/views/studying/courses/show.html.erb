<% title @course.name %>

<strong><%= @course.name %></strong>

<ul>
  <% @course.assignments.each do |assignment| %>
    <li><%= link_to assignment.name, studying_composition_path(@course, assignment) %></li>
  <% end %>
</ul>
